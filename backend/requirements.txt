fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
google-genai>=2.6.0
openai>=1.0.0
python-dotenv>=1.0.1
pydantic>=2.10.0
pydantic-settings>=2.7.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.13.0
python-jose[cryptography]>=3.3.0
bcrypt>=5.0.0
python-multipart>=0.0.9
apscheduler>=3.10.0
groq>=0.11.0
reportlab>=4.0.0
pillow>=10.1.0
boto3>=1.34.0
# pytesseract only binds to the `tesseract` binary — it does NOT install it.
# The Tesseract OCR engine itself must be installed separately on the host:
#   - macOS (local dev):  brew install tesseract
#   - Railway (prod):     tesseract-ocr added to the apt-get step in
#                          backend/Dockerfile (this repo builds via
#                          Dockerfile, not Nixpacks — see railway.json)
# Without the binary, ocr_service.detect_sku_from_image() fails on every call.
# If `pip install -r requirements.txt` hits an "externally-managed-environment"
# error outside the venv (e.g. Homebrew-managed system Python on macOS), install
# this package directly with:
#   pip install pytesseract --break-system-packages
pytesseract>=0.3.10
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
ruff>=0.8.0
